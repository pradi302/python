rows = 11

for i in range(rows):
    for j in range(rows):
    
        if j == 0 or j == rows - 1 or (i == j and j <= rows // 2) or (i + j == rows - 1 and j >= rows // 2):
            print("*", end=" ")
        else:
            print(" ", end=" ")
    print()
